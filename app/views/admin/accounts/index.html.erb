<div class="main__action">
  <div class="block-search">
    <%= search_form_for @q, url: admin_accounts_path, html: { class: "form-search" } do |f| %>
      <%= f.search_field :full_name_or_email_cont, class: "form-search__field", placeholder: "Tìm kiếm danh mục" %>
      <%= f.button :button, type: :submit, class: "form-search__submit" do %>
        <img src="<%= asset_path("icon/search.svg") %>" alt="icon-search">
      <% end %>
    <% end %>
  </div>
</div>

<% if @accounts.exists? %>
  <div class="data-list" data-post-target="category">
    <div class="data-list__main">
      <table class="table data-list__posts">
        <thead>
          <tr>
            <th class="checkmark-all">
              <label class="table__label">
                <input type="checkbox" class="checkbox-mark">
                <span class="checkmark"></span>
              </label>
            </th>
            <th>Ảnh</th>
            <th>
              <span>
                Thông tin người dùng
              </span>
            </th>
            <th>
              <span>
                Đường dẫn
              </span>
            </th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <%= render @accounts %>
        </tbody>
      </table>
      <div class="pagination">
        <%= render partial: 'shared/pagination', locals: { pagy: @pagy } %>
      </div>
    </div>
  </div>
<% else %>
  <div class="no-data">
    <div class="no-data__main">
      <span>Không có người dùng nào.</span>
    </div>
  </div>
<% end %>
