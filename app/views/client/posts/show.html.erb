<%= provide :title, @post.title %>
<%= provide :url, post_path(@post) %>
<%= provide :description, @post.description %>
<%= provide :image, url_for(@post.cover_image) if @post.cover_image.persisted? %>

<div class="autopagerize_page_element">
  <div class="content">
    <div class="post-page">
      <div class="post animated fadeInDown">
        <div class="post-title">
          <h1><span><b><%= @post.title %></b></span></h1>
        </div>
        
        <div class="post-footer">
          <div class="meta">
            <div class="info">
              <%= image_tag("icon/clock2.svg", alt: "icon clock") %> 
              <span class="date"> <%= @post.created_at.strftime(Settings.time.formats) %></span>

              <%= image_tag("icon/eye.svg", alt: "icon view") %> 
              <span class="date"> <%= @post.view_count %></span>
            </div>
          </div>
        </div>

        <div class="post-description">
          <p><%= @post.description %></p>
        </div>

        <div class="post-banner">
          <%= image_tag @post.cover_image, class: "size-100" %>
        </div>
        
        <div class="post-content">
          <%= raw @post.content %>
        </div>
      </div>
    </div>
  </div>
</div>
